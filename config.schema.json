{
  "pluginAlias": "NUTDashboard",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Monitor your UPS via NUT. **The live dashboard (voltage, battery, load, runtime history) is served as a standalone website** — set a **Standalone Dashboard Port** below, save, and restart Homebridge.",
  "footerDisplay": "**📊 Open the dashboard:** once a port is set and Homebridge has restarted, browse to `http://homebridge.local:PORT` or `http://localhost:PORT` (replace `PORT` with the value below). It works from any browser on your network — phone, tablet, or desktop.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "NUT UPS Dashboard",
        "required": true
      },
      "host": {
        "title": "NUT Server Host",
        "type": "string",
        "default": "127.0.0.1",
        "description": "Hostname or IP of the machine running upsd. Use 127.0.0.1 if NUT is on the same Pi."
      },
      "port": {
        "title": "NUT Server Port",
        "type": "integer",
        "default": 3493,
        "description": "TCP port upsd listens on (default 3493)."
      },
      "username": {
        "title": "NUT Username",
        "type": "string",
        "description": "Leave blank if upsmon.conf does not require authentication."
      },
      "password": {
        "title": "NUT Password",
        "type": "string",
        "description": "Leave blank if upsmon.conf does not require authentication."
      },
      "ups": {
        "title": "UPS Name(s)",
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [
          "ups"
        ],
        "description": "One or more UPS names as reported by 'upsc -l'. Typically just ['ups']."
      },
      "pollInterval": {
        "title": "Poll Interval (seconds)",
        "type": "integer",
        "default": 30,
        "minimum": 5,
        "description": "How often to query NUT for fresh data."
      },
      "lowBatteryThreshold": {
        "title": "Low Battery Threshold (%)",
        "type": "integer",
        "default": 20,
        "minimum": 1,
        "maximum": 50,
        "description": "Battery percentage below which the Low Battery alert triggers in HomeKit."
      },
      "standalonePort": {
        "title": "Standalone Dashboard Port",
        "type": "integer",
        "placeholder": 8581,
        "description": "Port to host the UPS dashboard as a local website (1–65535, e.g. 8581). Leave blank to disable. After saving and restarting Homebridge, open http://homebridge.local:8581 (or http://localhost:8581) — substituting the port you set here."
      }
    }
  },
  "layout": [
    {
      "key": "name"
    },
    {
      "type": "fieldset",
      "title": "NUT Connection",
      "items": [
        "host",
        "port",
        "username",
        "password"
      ]
    },
    {
      "type": "fieldset",
      "title": "UPS Settings",
      "items": [
        "ups",
        "pollInterval",
        "lowBatteryThreshold"
      ]
    },
    {
      "type": "fieldset",
      "title": "Standalone Dashboard",
      "description": "This is where you view your UPS data. Host the dashboard as a local website accessible from any browser on your network, then open it at http://homebridge.local:PORT.",
      "items": [
        "standalonePort"
      ]
    }
  ]
}
